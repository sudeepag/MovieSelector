<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.logitechs.movieselector">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <provider
            android:name="com.facebook.FacebookContentProvider"
            android:authorities="com.facebook.app.FacebookContentProvider1780275615534731"
            android:exported="true" />

        <activity
            android:name=".View.MuveeLogin"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.MuveeMainActivity"
            android:label="@string/app_name"
            android:parentActivityName=".View.MuveeLogin"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MuveeLogin" />
        </activity>
        <activity
            android:name=".View.MuveeRegistration"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Registration" />
        <activity
            android:name=".View.MuveeSettings"
            android:label="Settings"
            android:parentActivityName=".View.MuveeMainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Preference">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MuveeMainActivity" />
        </activity>
        <activity
            android:name=".View.MuveeDetails"
            android:label="Müveé Details"
            android:parentActivityName=".View.MuveeMainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.MovieDetails">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MuveeMainActivity" />
        </activity>
        <activity
            android:name=".View.MuveeAdminActivity"
            android:label="Müveé Administrator"
            android:parentActivityName=".View.MuveeLogin"
            android:theme="@style/AppTheme.MovieDetails">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MuveeLogin" />
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".View.MuveeMaps"
            android:label="@string/title_activity_muvee_maps"
            android:theme="@style/AppTheme.Maps">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.gatech.logitechs.movieselector.View.MuveeDetails" />
        </activity>
    </application>

</manifest>